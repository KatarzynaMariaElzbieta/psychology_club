apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: psychology-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: psychology.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: psychology-service
                port:
                  number: 80
